<?php


/**
 * Base class that represents a query for the 'siga_sala_log' table.
 *
 *
 *
 * This class was autogenerated by Propel 1.6.7 on:
 *
 * Wed Dec  4 10:55:32 2013
 *
 * @method RegistrosalaQuery orderById($order = Criteria::ASC) Order by the id column
 * @method RegistrosalaQuery orderBySalaId($order = Criteria::ASC) Order by the sala_id column
 * @method RegistrosalaQuery orderByMotivo($order = Criteria::ASC) Order by the motivo column
 * @method RegistrosalaQuery orderByInicio($order = Criteria::ASC) Order by the inicio column
 * @method RegistrosalaQuery orderByFin($order = Criteria::ASC) Order by the fin column
 * @method RegistrosalaQuery orderByDuracion($order = Criteria::ASC) Order by the duracion column
 *
 * @method RegistrosalaQuery groupById() Group by the id column
 * @method RegistrosalaQuery groupBySalaId() Group by the sala_id column
 * @method RegistrosalaQuery groupByMotivo() Group by the motivo column
 * @method RegistrosalaQuery groupByInicio() Group by the inicio column
 * @method RegistrosalaQuery groupByFin() Group by the fin column
 * @method RegistrosalaQuery groupByDuracion() Group by the duracion column
 *
 * @method RegistrosalaQuery leftJoin($relation) Adds a LEFT JOIN clause to the query
 * @method RegistrosalaQuery rightJoin($relation) Adds a RIGHT JOIN clause to the query
 * @method RegistrosalaQuery innerJoin($relation) Adds a INNER JOIN clause to the query
 *
 * @method Registrosala findOne(PropelPDO $con = null) Return the first Registrosala matching the query
 * @method Registrosala findOneOrCreate(PropelPDO $con = null) Return the first Registrosala matching the query, or a new Registrosala object populated from the query conditions when no match is found
 *
 * @method Registrosala findOneById(int $id) Return the first Registrosala filtered by the id column
 * @method Registrosala findOneBySalaId(int $sala_id) Return the first Registrosala filtered by the sala_id column
 * @method Registrosala findOneByMotivo(string $motivo) Return the first Registrosala filtered by the motivo column
 * @method Registrosala findOneByInicio(string $inicio) Return the first Registrosala filtered by the inicio column
 * @method Registrosala findOneByFin(string $fin) Return the first Registrosala filtered by the fin column
 * @method Registrosala findOneByDuracion(string $duracion) Return the first Registrosala filtered by the duracion column
 *
 * @method array findById(int $id) Return Registrosala objects filtered by the id column
 * @method array findBySalaId(int $sala_id) Return Registrosala objects filtered by the sala_id column
 * @method array findByMotivo(string $motivo) Return Registrosala objects filtered by the motivo column
 * @method array findByInicio(string $inicio) Return Registrosala objects filtered by the inicio column
 * @method array findByFin(string $fin) Return Registrosala objects filtered by the fin column
 * @method array findByDuracion(string $duracion) Return Registrosala objects filtered by the duracion column
 *
 * @package    propel.generator.lib.model.data.om
 */
abstract class BaseRegistrosalaQuery extends ModelCriteria
{
    /**
     * Initializes internal state of BaseRegistrosalaQuery object.
     *
     * @param     string $dbName The dabase name
     * @param     string $modelName The phpName of a model, e.g. 'Book'
     * @param     string $modelAlias The alias for the model in this query, e.g. 'b'
     */
    public function __construct($dbName = 'propel', $modelName = 'Registrosala', $modelAlias = null)
    {
        parent::__construct($dbName, $modelName, $modelAlias);
    }

    /**
     * Returns a new RegistrosalaQuery object.
     *
     * @param     string $modelAlias The alias of a model in the query
     * @param     RegistrosalaQuery|Criteria $criteria Optional Criteria to build the query from
     *
     * @return RegistrosalaQuery
     */
    public static function create($modelAlias = null, $criteria = null)
    {
        if ($criteria instanceof RegistrosalaQuery) {
            return $criteria;
        }
        $query = new RegistrosalaQuery();
        if (null !== $modelAlias) {
            $query->setModelAlias($modelAlias);
        }
        if ($criteria instanceof Criteria) {
            $query->mergeWith($criteria);
        }

        return $query;
    }

    /**
     * Find object by primary key.
     * Propel uses the instance pool to skip the database if the object exists.
     * Go fast if the query is untouched.
     *
     * <code>
     * $obj  = $c->findPk(12, $con);
     * </code>
     *
     * @param mixed $key Primary key to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return   Registrosala|Registrosala[]|mixed the result, formatted by the current formatter
     */
    public function findPk($key, $con = null)
    {
        if ($key === null) {
            return null;
        }
        if ((null !== ($obj = RegistrosalaPeer::getInstanceFromPool((string) $key))) && !$this->formatter) {
            // the object is alredy in the instance pool
            return $obj;
        }
        if ($con === null) {
            $con = Propel::getConnection(RegistrosalaPeer::DATABASE_NAME, Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        if ($this->formatter || $this->modelAlias || $this->with || $this->select
         || $this->selectColumns || $this->asColumns || $this->selectModifiers
         || $this->map || $this->having || $this->joins) {
            return $this->findPkComplex($key, $con);
        } else {
            return $this->findPkSimple($key, $con);
        }
    }

    /**
     * Find object by primary key using raw SQL to go fast.
     * Bypass doSelect() and the object formatter by using generated code.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return   Registrosala A model object, or null if the key is not found
     * @throws   PropelException
     */
    protected function findPkSimple($key, $con)
    {
        $sql = 'SELECT `ID`, `SALA_ID`, `MOTIVO`, `INICIO`, `FIN`, `DURACION` FROM `siga_sala_log` WHERE `ID` = :p0';
        try {
            $stmt = $con->prepare($sql);
            $stmt->bindValue(':p0', $key, PDO::PARAM_INT);
            $stmt->execute();
        } catch (Exception $e) {
            Propel::log($e->getMessage(), Propel::LOG_ERR);
            throw new PropelException(sprintf('Unable to execute SELECT statement [%s]', $sql), $e);
        }
        $obj = null;
        if ($row = $stmt->fetch(PDO::FETCH_NUM)) {
            $obj = new Registrosala();
            $obj->hydrate($row);
            RegistrosalaPeer::addInstanceToPool($obj, (string) $key);
        }
        $stmt->closeCursor();

        return $obj;
    }

    /**
     * Find object by primary key.
     *
     * @param     mixed $key Primary key to use for the query
     * @param     PropelPDO $con A connection object
     *
     * @return Registrosala|Registrosala[]|mixed the result, formatted by the current formatter
     */
    protected function findPkComplex($key, $con)
    {
        // As the query uses a PK condition, no limit(1) is necessary.
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKey($key)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->formatOne($stmt);
    }

    /**
     * Find objects by primary key
     * <code>
     * $objs = $c->findPks(array(12, 56, 832), $con);
     * </code>
     * @param     array $keys Primary keys to use for the query
     * @param     PropelPDO $con an optional connection object
     *
     * @return PropelObjectCollection|Registrosala[]|mixed the list of results, formatted by the current formatter
     */
    public function findPks($keys, $con = null)
    {
        if ($con === null) {
            $con = Propel::getConnection($this->getDbName(), Propel::CONNECTION_READ);
        }
        $this->basePreSelect($con);
        $criteria = $this->isKeepQuery() ? clone $this : $this;
        $stmt = $criteria
            ->filterByPrimaryKeys($keys)
            ->doSelect($con);

        return $criteria->getFormatter()->init($criteria)->format($stmt);
    }

    /**
     * Filter the query by primary key
     *
     * @param     mixed $key Primary key to use for the query
     *
     * @return RegistrosalaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKey($key)
    {

        return $this->addUsingAlias(RegistrosalaPeer::ID, $key, Criteria::EQUAL);
    }

    /**
     * Filter the query by a list of primary keys
     *
     * @param     array $keys The list of primary key to use for the query
     *
     * @return RegistrosalaQuery The current query, for fluid interface
     */
    public function filterByPrimaryKeys($keys)
    {

        return $this->addUsingAlias(RegistrosalaPeer::ID, $keys, Criteria::IN);
    }

    /**
     * Filter the query on the id column
     *
     * Example usage:
     * <code>
     * $query->filterById(1234); // WHERE id = 1234
     * $query->filterById(array(12, 34)); // WHERE id IN (12, 34)
     * $query->filterById(array('min' => 12)); // WHERE id > 12
     * </code>
     *
     * @param     mixed $id The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RegistrosalaQuery The current query, for fluid interface
     */
    public function filterById($id = null, $comparison = null)
    {
        if (is_array($id) && null === $comparison) {
            $comparison = Criteria::IN;
        }

        return $this->addUsingAlias(RegistrosalaPeer::ID, $id, $comparison);
    }

    /**
     * Filter the query on the sala_id column
     *
     * Example usage:
     * <code>
     * $query->filterBySalaId(1234); // WHERE sala_id = 1234
     * $query->filterBySalaId(array(12, 34)); // WHERE sala_id IN (12, 34)
     * $query->filterBySalaId(array('min' => 12)); // WHERE sala_id > 12
     * </code>
     *
     * @param     mixed $salaId The value to use as filter.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RegistrosalaQuery The current query, for fluid interface
     */
    public function filterBySalaId($salaId = null, $comparison = null)
    {
        if (is_array($salaId)) {
            $useMinMax = false;
            if (isset($salaId['min'])) {
                $this->addUsingAlias(RegistrosalaPeer::SALA_ID, $salaId['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($salaId['max'])) {
                $this->addUsingAlias(RegistrosalaPeer::SALA_ID, $salaId['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RegistrosalaPeer::SALA_ID, $salaId, $comparison);
    }

    /**
     * Filter the query on the motivo column
     *
     * Example usage:
     * <code>
     * $query->filterByMotivo('fooValue');   // WHERE motivo = 'fooValue'
     * $query->filterByMotivo('%fooValue%'); // WHERE motivo LIKE '%fooValue%'
     * </code>
     *
     * @param     string $motivo The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RegistrosalaQuery The current query, for fluid interface
     */
    public function filterByMotivo($motivo = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($motivo)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $motivo)) {
                $motivo = str_replace('*', '%', $motivo);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RegistrosalaPeer::MOTIVO, $motivo, $comparison);
    }

    /**
     * Filter the query on the inicio column
     *
     * Example usage:
     * <code>
     * $query->filterByInicio('2011-03-14'); // WHERE inicio = '2011-03-14'
     * $query->filterByInicio('now'); // WHERE inicio = '2011-03-14'
     * $query->filterByInicio(array('max' => 'yesterday')); // WHERE inicio > '2011-03-13'
     * </code>
     *
     * @param     mixed $inicio The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RegistrosalaQuery The current query, for fluid interface
     */
    public function filterByInicio($inicio = null, $comparison = null)
    {
        if (is_array($inicio)) {
            $useMinMax = false;
            if (isset($inicio['min'])) {
                $this->addUsingAlias(RegistrosalaPeer::INICIO, $inicio['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($inicio['max'])) {
                $this->addUsingAlias(RegistrosalaPeer::INICIO, $inicio['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RegistrosalaPeer::INICIO, $inicio, $comparison);
    }

    /**
     * Filter the query on the fin column
     *
     * Example usage:
     * <code>
     * $query->filterByFin('2011-03-14'); // WHERE fin = '2011-03-14'
     * $query->filterByFin('now'); // WHERE fin = '2011-03-14'
     * $query->filterByFin(array('max' => 'yesterday')); // WHERE fin > '2011-03-13'
     * </code>
     *
     * @param     mixed $fin The value to use as filter.
     *              Values can be integers (unix timestamps), DateTime objects, or strings.
     *              Empty strings are treated as NULL.
     *              Use scalar values for equality.
     *              Use array values for in_array() equivalent.
     *              Use associative array('min' => $minValue, 'max' => $maxValue) for intervals.
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RegistrosalaQuery The current query, for fluid interface
     */
    public function filterByFin($fin = null, $comparison = null)
    {
        if (is_array($fin)) {
            $useMinMax = false;
            if (isset($fin['min'])) {
                $this->addUsingAlias(RegistrosalaPeer::FIN, $fin['min'], Criteria::GREATER_EQUAL);
                $useMinMax = true;
            }
            if (isset($fin['max'])) {
                $this->addUsingAlias(RegistrosalaPeer::FIN, $fin['max'], Criteria::LESS_EQUAL);
                $useMinMax = true;
            }
            if ($useMinMax) {
                return $this;
            }
            if (null === $comparison) {
                $comparison = Criteria::IN;
            }
        }

        return $this->addUsingAlias(RegistrosalaPeer::FIN, $fin, $comparison);
    }

    /**
     * Filter the query on the duracion column
     *
     * Example usage:
     * <code>
     * $query->filterByDuracion('fooValue');   // WHERE duracion = 'fooValue'
     * $query->filterByDuracion('%fooValue%'); // WHERE duracion LIKE '%fooValue%'
     * </code>
     *
     * @param     string $duracion The value to use as filter.
     *              Accepts wildcards (* and % trigger a LIKE)
     * @param     string $comparison Operator to use for the column comparison, defaults to Criteria::EQUAL
     *
     * @return RegistrosalaQuery The current query, for fluid interface
     */
    public function filterByDuracion($duracion = null, $comparison = null)
    {
        if (null === $comparison) {
            if (is_array($duracion)) {
                $comparison = Criteria::IN;
            } elseif (preg_match('/[\%\*]/', $duracion)) {
                $duracion = str_replace('*', '%', $duracion);
                $comparison = Criteria::LIKE;
            }
        }

        return $this->addUsingAlias(RegistrosalaPeer::DURACION, $duracion, $comparison);
    }

    /**
     * Exclude object from result
     *
     * @param   Registrosala $registrosala Object to remove from the list of results
     *
     * @return RegistrosalaQuery The current query, for fluid interface
     */
    public function prune($registrosala = null)
    {
        if ($registrosala) {
            $this->addUsingAlias(RegistrosalaPeer::ID, $registrosala->getId(), Criteria::NOT_EQUAL);
        }

        return $this;
    }

}
